apiVersion: v1
kind: Pod
metadata:
    name: mypod
    labels:
        type: app
spec:
    containers:
        - name: webserver
          image: nginx
          ports:
            - containerPort: 80
